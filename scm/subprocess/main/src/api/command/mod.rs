//! Command normalisation contracts — SEA Rule 121 api/ counterpart for `core/command/`.
//!
//! The `NormalisedCommand` implementation lives in `core/command/` and is `pub(crate)`.
//! This module anchors the api/core pairing required by SEA Rule 121.
