//! Extension adapter contracts — SEA Rule 121 api/ counterpart for `core/extension/`.
//!
//! The `ExtensionRunner` adapter lives in `core/extension/` and is `pub(crate)`.
//! This module anchors the api/core pairing required by SEA Rule 121.
