//! Default implementation contracts — SEA Rule 121 api/ counterpart for `core/default/`.
//!
//! The concrete types live in `core/default/` and are `pub(crate)`.
//! This module anchors the api/core pairing required by SEA Rule 121.
